name: ${project.name}
description: ${project.description}
version: ${project.version}
source-version: ${githead.branch}-${githead.commit}
provided-services:
- de.cubeisland.engine.core.service.Economy
