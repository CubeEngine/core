name: ${project.name}
description: ${project.description}
version: ${project.version}
source-version: ${githead.branch}-${githead.commit}
plugin-dependencies:
- Vault
soft-dependencies:
- Conomy
- Chat
- Roles
