name: ${project.name}
description: ${project.description}
version: ${project.version}
source-version: ${githead.branch}-${githead.commit}
dependencies:
- conomy
services:
- de.cubeisland.cubeengine.core.service.Economy